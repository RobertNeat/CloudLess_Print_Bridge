# Infrastructure

Katalog zawiera pliki służące do uruchamiania i łączenia podprojektów w kontenerach.

Planowana zawartość:

```text
infrastructure/
├── docker/
│   ├── video-service-hub.Dockerfile
│   ├── ftps-remote-manager.Dockerfile
│   ├── mqtt-puppeteer.Dockerfile
│   └── octo-management-dashboard.Dockerfile
├── compose/
│   ├── docker-compose.yml
│   └── docker-compose.override.yml
├── proxy/
│   └── nginx.conf
└── env/
    └── .env.example
```
